name: Apply medium audit fixes once

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/apply-medium-fixes-once.yml'

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout medium branch
        uses: actions/checkout@v4
        with:
          ref: fix/medium-audit-v1
          fetch-depth: 0

      - name: Apply targeted fixes
        shell: bash
        run: |
          python <<'PY'
          import re
          from pathlib import Path

          def replace_function(text: str, name: str, next_name: str, body: str) -> str:
              pattern = rf"    function {re.escape(name)}\([^\n]*\) \{{.*?(?=    (?:function|match) {re.escape(next_name)})"
              updated, count = re.subn(pattern, body.rstrip() + '\n\n', text, count=1, flags=re.S)
              if count != 1:
                  raise SystemExit(f'Unable to replace {name}: {count}')
              return updated

          rules_path = Path('firestore.secure.rules')
          rules = rules_path.read_text()

          rules = replace_function(
              rules,
              'validNewProfile',
              'safeSelfUpdate',
              '''    function validNewProfile(userId) {
                return request.resource.data.keys().hasOnly([
                    'uid', 'firstName', 'lastName', 'displayName', 'email', 'role',
                    'status', 'statusBeforeBlock', 'avatar', 'fieldOfStudy',
                    'studyYear', 'title', 'specialization', 'subjects',
                    'institution', 'experience', 'bio', 'city', 'lessonPrice',
                    'lessonDuration', 'lessonFormats', 'timezone', 'rating',
                    'reviewsCount', 'isPublic', 'notificationPreferences',
                    'moderationNote', 'moderatedBy', 'moderatedAt',
                    'onboardingCompleted', 'createdAt', 'updatedAt'
                  ])
                  && request.resource.data.keys().hasAll([
                    'uid', 'firstName', 'lastName', 'displayName', 'email', 'role',
                    'status', 'avatar', 'rating', 'reviewsCount', 'isPublic',
                    'onboardingCompleted', 'createdAt', 'updatedAt'
                  ])
                  && request.resource.data.uid == userId
                  && request.resource.data.email == request.auth.token.email
                  && request.resource.data.firstName is string
                  && request.resource.data.firstName.size() > 0
                  && request.resource.data.firstName.size() <= 80
                  && request.resource.data.lastName is string
                  && request.resource.data.lastName.size() > 0
                  && request.resource.data.lastName.size() <= 80
                  && request.resource.data.displayName is string
                  && request.resource.data.displayName.size() > 0
                  && request.resource.data.displayName.size() <= 160
                  && request.resource.data.avatar is string
                  && request.resource.data.avatar.size() <= 2000
                  && request.resource.data.rating == 0
                  && request.resource.data.reviewsCount == 0
                  && request.resource.data.isPublic == false
                  && request.resource.data.onboardingCompleted is bool
                  && request.resource.data.createdAt is timestamp
                  && request.resource.data.updatedAt is timestamp
                  && (
                    (
                      request.resource.data.role == 'student'
                      && request.resource.data.status == 'active'
                    )
                    || (
                      request.resource.data.role == 'tutor'
                      && request.resource.data.status == 'pending'
                    )
                  );
              }''',
          )

          rules = replace_function(
              rules,
              'safeSelfUpdate',
              'validTutorResubmission',
              '''    function safeSelfUpdate() {
                return request.resource.data.uid == resource.data.uid
                  && request.resource.data.email == resource.data.email
                  && request.resource.data.diff(resource.data).affectedKeys().hasOnly([
                    'firstName', 'lastName', 'displayName', 'avatar',
                    'fieldOfStudy', 'studyYear', 'title', 'specialization',
                    'subjects', 'institution', 'experience', 'bio', 'city',
                    'lessonPrice', 'lessonDuration', 'lessonFormats', 'timezone',
                    'notificationPreferences', 'onboardingCompleted', 'updatedAt'
                  ]);
              }''',
          )

          rules = replace_function(
              rules,
              'validAvailability',
              '/availability/{tutorId}',
              '''    function validAvailability(data, tutorId) {
                return data.keys().hasOnly(['tutorUid', 'timezone', 'days', 'updatedAt'])
                  && data.tutorUid == tutorId
                  && data.timezone is string
                  && data.timezone.size() > 0
                  && data.timezone.size() <= 80
                  && data.days is map
                  && data.days.size() <= 7
                  && data.updatedAt is timestamp;
              }''',
          )

          kind_line = "        && request.resource.data.kind in ['link', 'document', 'video', 'note']"
          https_check = """        && (
                  (request.resource.data.kind == 'note' && request.resource.data.url == '')
                  || (
                    request.resource.data.kind in ['link', 'document', 'video']
                    && request.resource.data.url.matches('https://.+')
                  )
                )"""
          if rules.count(kind_line) != 1:
              raise SystemExit(f'material create kind line count: {rules.count(kind_line)}')
          rules = rules.replace(kind_line, kind_line + '\n' + https_check, 1)

          material_start = rules.index('    match /materials/{materialId} {')
          material_end = rules.index('    function validKnowledgeSubmissionCreate', material_start)
          material = rules[material_start:material_end]
          update_anchor = """      allow update: if isActiveRole(request.auth.uid, 'tutor')
                && resource.data.tutorUid == request.auth.uid
                && request.resource.data.updatedAt is timestamp"""
          update_extra = """
                && request.resource.data.kind in ['link', 'document', 'video', 'note']
                && request.resource.data.url is string
                && request.resource.data.url.size() <= 2000
                && (
                  (request.resource.data.kind == 'note' && request.resource.data.url == '')
                  || (
                    request.resource.data.kind in ['link', 'document', 'video']
                    && request.resource.data.url.matches('https://.+')
                  )
                )"""
          if material.count(update_anchor) != 1:
              raise SystemExit(f'material update anchor count: {material.count(update_anchor)}')
          material = material.replace(update_anchor, update_anchor + update_extra, 1)
          rules = rules[:material_start] + material + rules[material_end:]

          rules = replace_function(
              rules,
              'canReadLessonData',
              'canWriteLessonData',
              '''    function canReadLessonData(bookingId) {
                return bookingExists(bookingId)
                  && (
                    isModerator()
                    || (
                      isActiveLessonParticipant(bookingId)
                      && (
                        whiteboardBooking(bookingId).status == 'accepted'
                        || (
                          whiteboardBooking(bookingId).status == 'completed'
                          && whiteboardBooking(bookingId).completedAt is timestamp
                          && request.time
                            < whiteboardBooking(bookingId).completedAt
                              + duration.value(180, 'd')
                        )
                      )
                    )
                  );
              }''',
          )
          rules_path.write_text(rules)

          storage_path = Path('storage.rules')
          storage = storage_path.read_text()
          storage_pattern = r"    function canReadMedicalAsset\(bookingId\) \{.*?(?=    match /medical-workspaces/)"
          storage_body = '''    function canReadMedicalAsset(bookingId) {
                return bookingExists(bookingId)
                  && (
                    isModerator()
                    || (
                      isActiveMedicalParticipant(bookingId)
                      && (
                        medicalBooking(bookingId).status == 'accepted'
                        || (
                          medicalBooking(bookingId).status == 'completed'
                          && medicalBooking(bookingId).completedAt is timestamp
                          && request.time
                            < medicalBooking(bookingId).completedAt
                              + duration.value(180, 'd')
                        )
                      )
                    )
                  );
              }

'''
          storage, count = re.subn(storage_pattern, storage_body, storage, count=1, flags=re.S)
          if count != 1:
              raise SystemExit(f'storage read replacement: {count}')
          storage_path.write_text(storage)

          workspace_path = Path('artifacts/medstart/components/live/MedicalWorkspace.tsx')
          workspace = workspace_path.read_text()
          helper_end = """function anatomyDataUri(layer: AnatomyLayer, region: string, view: AnatomyView) {
            return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(
              anatomySvgMarkup(layer, region, view),
            )}`
          }
          """
          parser = """
          function parseOptionalNumber(value: string) {
            const normalized = value.trim().replace(',', '.')
            if (!normalized) return null
            const parsed = Number(normalized)
            return Number.isFinite(parsed) ? parsed : null
          }
          """
          if workspace.count(helper_end) != 1:
              raise SystemExit(f'anatomy helper count: {workspace.count(helper_end)}')
          workspace = workspace.replace(helper_end, helper_end + parser, 1)

          lab_pattern = re.compile(
              r"                const numericValue = Number\(row\.value\.replace\(',', '\.'\)\).*?                      : 'В пределах диапазона'",
              re.S,
          )
          lab_replacement = """                const numericValue = parseOptionalNumber(row.value)
                          const low = parseOptionalNumber(row.referenceLow)
                          const high = parseOptionalNumber(row.referenceHigh)
                          const flag =
                            numericValue === null
                              ? 'Не заполнено'
                              : low !== null && numericValue < low
                                ? 'Ниже нормы'
                                : high !== null && numericValue > high
                                  ? 'Выше нормы'
                                  : low === null && high === null
                                    ? 'Нет референсов'
                                    : 'В пределах диапазона'"""
          workspace, count = lab_pattern.subn(lab_replacement, workspace, count=1)
          if count != 1:
              raise SystemExit(f'lab calculation replacement: {count}')

          old_badge = """                          flag === 'В пределах диапазона'
                                      ? 'bg-emerald-500/15 text-emerald-300'
                                      : 'bg-amber-500/15 text-amber-200'"""
          new_badge = """                          flag === 'В пределах диапазона'
                                      ? 'bg-emerald-500/15 text-emerald-300'
                                      : flag === 'Ниже нормы' || flag === 'Выше нормы'
                                        ? 'bg-amber-500/15 text-amber-200'
                                        : 'bg-slate-500/15 text-slate-300'"""
          if workspace.count(old_badge) != 1:
              raise SystemExit(f'lab badge count: {workspace.count(old_badge)}')
          workspace = workspace.replace(old_badge, new_badge, 1)

          ecg_anchor = """              </svg>
                        </div>
                        <div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-4">"""
          ecg_replacement = """              </svg>
                        </div>
                        <p className="mt-3 rounded-2xl border border-amber-300/20 bg-amber-400/10 px-4 py-3 text-xs leading-5 text-amber-100">
                          Кривая генерируется как учебная иллюстрация и не является клинической ЭКГ, диагностикой или медицинским заключением.
                        </p>
                        <div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-4">"""
          if workspace.count(ecg_anchor) != 1:
              raise SystemExit(f'ECG anchor count: {workspace.count(ecg_anchor)}')
          workspace = workspace.replace(ecg_anchor, ecg_replacement, 1)
          workspace_path.write_text(workspace)
          PY

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.26.1

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Firebase rules
        run: >-
          pnpm dlx firebase-tools@15.24.0 emulators:exec
          --only firestore,storage
          --project demo-medstart
          "node scripts/test-critical-rules.mjs"

      - name: Typecheck
        run: pnpm --filter @workspace/medstart typecheck

      - name: Build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: test-api-key
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: example.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: example
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: example.appspot.com
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '123456789'
          NEXT_PUBLIC_FIREBASE_APP_ID: 1:123456789:web:test
          NEXT_PUBLIC_OWNER_UID: test-owner
        run: pnpm --filter @workspace/medstart build

      - name: Commit validated fixes and remove temporary workflows
        shell: bash
        run: |
          rm -f .github/workflows/apply-medium-fixes-once.yml
          rm -f .github/workflows/medium-diagnostics.yml
          rm -f .github/workflows/medium-migration-diagnostics.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: resolve remaining medium audit findings"
          git push origin HEAD:fix/medium-audit-v1
