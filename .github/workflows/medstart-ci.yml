name: MedStart CI

on:
  push:
    branches:
      - main
      - 'fix/**'
      - 'security/**'
    paths:
      - 'artifacts/medstart/**'
      - 'scripts/**'
      - 'firestore.secure.rules'
      - 'storage.rules'
      - 'firebase.json'
      - 'firestore.indexes.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - 'tsconfig*.json'
      - '.github/workflows/**'
  pull_request:
    branches:
      - main
    paths:
      - 'artifacts/medstart/**'
      - 'scripts/**'
      - 'firestore.secure.rules'
      - 'storage.rules'
      - 'firebase.json'
      - 'firestore.indexes.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
      - 'tsconfig*.json'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: medstart-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.26.1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Set up Java for Firebase emulators
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Verify hardened Firebase configuration
        run: |
          test -f firestore.secure.rules
          test -f storage.rules
          # Legacy root rules must never become a second deployment source.
          test ! -e firestore.rules
          grep -q '"rules": "firestore.secure.rules"' firebase.json
          grep -q 'allow create, update: if false;' storage.rules
          grep -A2 'match /users/{userId}' firestore.secure.rules | grep -q 'allow create: if false;'
          grep -A2 'match /bookings/{bookingId}' firestore.secure.rules | grep -q 'allow create: if false;'
          grep -A2 'match /conversations/{conversationId}' firestore.secure.rules | grep -q 'allow create: if false;'
          grep -A16 'match /knowledgeSubmissions/{submissionId}' firestore.secure.rules | grep -q 'allow create: if false;'
          grep -A16 'match /knowledgeSubmissions/{submissionId}' firestore.secure.rules | grep -q 'allow update, delete: if false;'
          grep -A3 'match /avatars/' storage.rules | grep -q 'allow create, update, delete: if false;'
          grep -A3 'match /knowledge-submissions/' storage.rules | grep -q 'allow read, write: if false;'
          grep -A3 'match /knowledge-quarantine/' storage.rules | grep -q 'allow read, write: if false;'
          grep -A3 'match /knowledge-published/' storage.rules | grep -q 'allow read, write: if false;'
          grep -q "direct-client-profile-create-blocked" artifacts/medstart/scripts/live-auth-audit.mjs
          ! grep -q 'medstart-board-' artifacts/medstart/components/live/ServerlessWhiteboard.tsx
          grep -q 'canApplyRealtimeElement' artifacts/medstart/components/live/ServerlessWhiteboard.tsx
          test -f artifacts/medstart/app/api/bookings/route.ts
          test -f artifacts/medstart/app/api/messages/action/route.ts
          test -f artifacts/medstart/app/api/messages/media/route.ts
          test -f artifacts/medstart/app/api/profile/avatar/route.ts
          test -f artifacts/medstart/app/api/knowledge/files/route.ts
          test -f artifacts/medstart/app/api/knowledge/submissions/route.ts
          test -f artifacts/medstart/app/api/knowledge/moderation/route.ts
          test -f artifacts/medstart/lib/server/file-security.ts
          test -f artifacts/medstart/lib/server/knowledge-access.ts
          test -f artifacts/medstart/lib/server/knowledge-security.ts
          test -f scripts/test-file-security.mjs
          test -f scripts/test-avatar-rules.mjs
          test -f scripts/test-live-whiteboard-security.mjs
          test -f scripts/test-knowledge-pipeline.mjs
          test -f scripts/test-knowledge-rules.mjs
          test -f scripts/test-live-audit-guard.mjs
          grep -q "fetch('/api/bookings'" artifacts/medstart/lib/bookings.ts
          grep -q "fetch('/api/messages/action'" artifacts/medstart/lib/conversations.ts
          grep -q "'/api/messages/media'" artifacts/medstart/lib/chat-media.ts
          grep -q "fetch('/api/profile/avatar'" artifacts/medstart/lib/storage.ts
          grep -q 'detectUploadType' artifacts/medstart/app/api/profile/avatar/route.ts
          grep -q "'/api/knowledge/files'" artifacts/medstart/lib/knowledge-upload.ts
          grep -q "'/api/knowledge/submissions'" artifacts/medstart/lib/knowledge-base.ts
          grep -q "'/api/knowledge/moderation'" artifacts/medstart/lib/knowledge-base.ts
          ! grep -q "from 'firebase/storage'" artifacts/medstart/lib/storage.ts
          ! grep -R -q "from 'firebase/storage'" artifacts/medstart/lib/chat-media.ts artifacts/medstart/components/messages artifacts/medstart/app/dashboard/messages artifacts/medstart/lib/knowledge-upload.ts
          ! grep -q 'updateDoc(' artifacts/medstart/lib/knowledge-base.ts
          test -f scripts/test-critical-rules.mjs
          test -f scripts/test-medium-rules.mjs
          test -f scripts/test-auth-rules.mjs
          test -f scripts/test-message-rules.mjs
          test -f scripts/final-audit-invariants.mjs
          test -f scripts/full-repository-audit.mjs
          test -f scripts/button-system-audit.mjs
          test -f scripts/mobile-messenger-invariants.mjs

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level high

      - name: Run full repository audit
        run: node scripts/full-repository-audit.mjs

      - name: Audit unified button system
        run: node scripts/button-system-audit.mjs

      - name: Run final static audit invariants
        run: node scripts/final-audit-invariants.mjs

      - name: Test server file security
        run: node scripts/test-file-security.mjs

      - name: Test realtime whiteboard security
        run: node scripts/test-live-whiteboard-security.mjs

      - name: Test trusted knowledge pipeline
        run: |
          node scripts/test-knowledge-pipeline.mjs
          node scripts/test-live-audit-guard.mjs
          pnpm --dir artifacts/medstart exec node scripts/verify-knowledge-rules.mjs

      - name: Audit mobile messenger invariants
        run: node scripts/mobile-messenger-invariants.mjs

      - name: Compile and test Firebase security rules
        env:
          CI: 'true'
        run: >-
          pnpm dlx firebase-tools@15.24.0 emulators:exec
          --only firestore,storage
          --project demo-medstart
          "node scripts/test-critical-rules.mjs && node scripts/test-medium-rules.mjs && node scripts/test-auth-rules.mjs && node scripts/test-message-rules.mjs && node scripts/test-knowledge-rules.mjs && node scripts/test-avatar-rules.mjs"

      - name: Lint MedStart
        run: pnpm --filter @workspace/medstart lint

      - name: Typecheck MedStart
        shell: bash
        run: |
          set -o pipefail
          pnpm --filter @workspace/medstart typecheck 2>&1 | tee typecheck.log

      - name: Upload typecheck report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: medstart-typecheck
          path: typecheck.log
          if-no-files-found: warn
          retention-days: 3

      - name: Build MedStart
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: test-api-key
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: example.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: example
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: example.appspot.com
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '123456789'
          NEXT_PUBLIC_FIREBASE_APP_ID: 1:123456789:web:test
          NEXT_PUBLIC_OWNER_UID: test-owner
        run: pnpm --filter @workspace/medstart build
